apiVersion: v1
kind: Service
metadata:
  name: dm-frontend-service
  namespace: frontend
  labels:
    app: dm-frontend-app
spec:
  selector:
    app: dm-frontend-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort