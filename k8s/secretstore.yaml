apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: dm-secretstore
spec:
  provider:
    aws:
      service: SecretsManager
      region: eu-north-1
      auth:
        jwt:
          serviceAccountRef:
            name: dm-frontend-sa